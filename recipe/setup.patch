diff --git a/setup.py b/setup.py
index d8347ad..779c664 100644
--- a/setup.py
+++ b/setup.py
@@ -11,11 +11,11 @@ from setuptools import setup
 
 
 # Compilation options
-enable_MKL = True
+enable_MKL = False
 debug = False
 force = True # to force recompilation
 annotate = debug # to generate annotation (HTML files)
-useOpenmp = True
+useOpenmp = False
 
 
 # Cython modules
@@ -30,7 +30,7 @@ try:
     # Compiler-dependent configuration
     # See https://stackoverflow.com/questions/30985862
     BUILD_ARGS = {}
-    for compiler in ('msvc', 'gcc', 'icc'):
+    for compiler in ('msvc', 'gcc', 'icc', os.path.basename(os.environ.get('CXX', 'cpp'))):
         if debug:
             compile_args = ['-g','-O', '-std=c++11']
         else:
